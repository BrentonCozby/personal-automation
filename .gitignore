node_modules/
**/dist/
**/audit/
# apps/tasks/runs: the touch clock, the cap-raise record, and the per-run tuning logs. They carry
# real task titles (personal data), and nothing rebuilds them from the repo.
**/runs/
.env
.env.local
gmail-api-credentials.json
*.log
# newsyslog rotates launchd logs to *.log.0.gz … *.log.3.gz (Z flag).
*.log.*
.DS_Store

# Generated by launchd/setup.sh: these carry machine-specific paths (.template/.sh stay tracked).
launchd/*.plist
launchd/newsyslog.personal-automation.conf
coverage/
.tsbuildinfo
**/*.tsbuildinfo
