node_modules/
**/dist/
**/audit/
# stalled-tasks tuning records — contain real reminder titles (personal data).
**/runs/
# stalled-tasks Reminders bridge — compiled on demand from reminders.swift (platform-specific).
apps/stalled-tasks/src/tasks/apple/reminders-bridge
.env
.env.local
gmail-api-credentials.json
*.log
# newsyslog rotates launchd logs to *.log.0.gz … *.log.3.gz (Z flag).
*.log.*
.DS_Store

# Generated by launchd/setup.sh — contain machine-specific paths (.template/.sh stay tracked).
launchd/*.plist
launchd/newsyslog.personal-automation.conf
coverage/
.tsbuildinfo
**/*.tsbuildinfo
