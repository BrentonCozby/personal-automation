# notify config. Gmail OAuth credentials are shared: they live in the
# monorepo-root .env.

# Recipient for the daily error-digest email. Sent from the authenticated Gmail
# account (root .env); From-address is implicit.
NOTIFY_TO_EMAIL=
